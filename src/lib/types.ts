export type Pet = {
  id: string;
  name: string;
  imageUrl: string;
  ownerName: string;
  age: number;
  notes: string;
};

export type Default = {
  children: React.ReactNode;
  className?: string;
};
